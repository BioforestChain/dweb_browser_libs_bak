namespace reverse_proxy {
    void start(string frontend_certs_path, string frontend_key_path, u16 backend_port, VoidCallback on_ready);
};

callback interface VoidCallback {
    void callback(u16 proxy_port, u16 frontend_port);
};
